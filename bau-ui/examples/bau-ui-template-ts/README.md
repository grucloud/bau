# Bau UI Typescript Template

Minimal example for the Bau UI component library in Typescript.

## Workflow

Install the dependencies:

```sh
npm install
```

Start a development server:

```sh
npm run dev
```

Build a production version:

```sh
npm run build
```
